To find the year and the day, I will first check if they year is a leap year because if it is,
the calculations will be different than if it is not a leap year. Then, if it is not a leap year, I will us
a function that will subtract each month's amount of days depending on the number given
    ie - if days == 36, I will subtract 31 to take away the days of January
If it is a leap year, I will call the same function but add 1 if the day is above 59(Jan + Normal Feb).
Then I will call create a similar function for leap year days by just adding 1 to the ranges of the months in the
regular year.



def start():
    def valid_year(year):
        if year > 0:
            return True
        else:
            return False

    def valid_day_of_year(year, day_of_year):
        if year % 4 == 0 and year % 100 != 0 or year % 400 == 0:
            if day_of_year > 0 and day_of_year <= 366 :
                return True
            else:
                return False
        else:
            if day_of_year > 0 and day_of_year <= 365:
                return True
            else:
                return False

    def is_leap_year(n):
        '''
        Given an int, return whether that value is a leap year
        '''
        if n % 4 != 0:
            return False
        if n % 100 != 0:
            return True
        if n % 400 != 0:
            return False
        else:
            return True
    def input_year():
        year_var = int(input("Enter a year above 0: "))
        year = valid_year(year_var)
        valid_year(year)
        if valid_year(year):
            return year
        else:
            return False


    def input_day_of_year(year):
        day_var = int(input("Enter a day above 0: "))




#start()
    if year % 4 == 0:
        if year % 100 == 0:
            if year % 400 == 0:
                return True
        return True